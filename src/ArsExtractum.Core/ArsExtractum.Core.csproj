<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>ArsExtractum.Core</RootNamespace>
    <AssemblyName>ArsExtractum.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <EmbeddedResource Include="LaboratorySemantic/Catalog/*.json" />
    <!-- v0 remains on disk only as historical reference; it is not productive code. -->
    <Compile Remove="Semantic/**/*.cs" />
  </ItemGroup>
</Project>
